import { createX402LLM, CreateX402LLMOptions } from '@lucid-agents/payments';

type AxLLMClientOptions = {
    provider?: string;
    model?: string;
    apiKey?: string;
    apiUrl?: string;
    temperature?: number;
    debug?: boolean;
    env?: Record<string, string | undefined>;
    client?: ReturnType<typeof createX402LLM>;
    clientFactory?: () => ReturnType<typeof createX402LLM> | null;
    x402?: CreateX402LLMOptions;
    logger?: {
        warn?: (message: string, error?: unknown) => void;
    };
};
type AxLLMClient = {
    ax: ReturnType<typeof createX402LLM> | null;
    isConfigured(): boolean;
};
declare function createAxLLMClient(options?: AxLLMClientOptions): AxLLMClient;

export { type AxLLMClient, type AxLLMClientOptions, createAxLLMClient };
